version: "3.9"

networks:
  mynet:
    name: mynet
    driver: bridge
    ipam:
     config:
       - subnet: 10.5.0.0/16
         gateway: 10.5.0.1

services:
  state_checker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: state_checker
    volumes:
      - .:/code  # <--  THIS line enables hot code reloading! and writes logs back to source Dir.
    stdin_open: true # docker run -i
    tty: true        # docker run -t
    networks:
      - mynet
    ports:
      - "${REST_API_PORT}:${REST_API_PORT}"
    command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "${REST_API_PORT}"]
